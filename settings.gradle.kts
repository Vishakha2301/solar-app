// Required by Flutter's Gradle plugin when flutter.source points at the
// repository root from android/app/build.gradle.kts.
//
// The Android build continues to be configured from android/settings.gradle.kts.
rootProject.name = "solar-app"
